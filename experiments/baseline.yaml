# Qlib baseline config for qrun
# Based on Qlib workflow config examples.

qlib_init:
  provider_uri: "~/.qlib/qlib_data/jp_data"
  # Qlib officially supports region: cn/us. Use one and override trading settings for JP as needed.
  region: "cn"

market: &market all
benchmark: &benchmark 7203.T  # Set to a symbol present in your JP data

# Data handler config
handler_config: &handler_config
  start_time: 2015-01-05
  end_time: 2026-03-18
  fit_start_time: 2015-01-05
  fit_end_time: 2020-12-31
  instruments: *market

# Dataset segments
segments: &segments
  train: [2015-01-05, 2020-12-31]
  valid: [2021-01-01, 2022-12-31]
  test: [2023-01-01, 2026-03-18]

# Main task

task:
  model:
    class: LGBModel
    module_path: qlib.contrib.model.gbdt
    kwargs:
      loss: mse
      learning_rate: 0.05
      num_leaves: 210
      max_depth: 8
      subsample: 0.9
      colsample_bytree: 0.9
      lambda_l1: 200
      lambda_l2: 600
      num_threads: 8
  dataset:
    class: DatasetH
    module_path: qlib.data.dataset
    kwargs:
      handler:
        class: Alpha158
        module_path: qlib.contrib.data.handler
        kwargs: *handler_config
      segments: *segments
  record:
    - class: SignalRecord
      module_path: qlib.workflow.record_temp
      kwargs: {}
